#!/bin/bash

convert \
    -font /usr/share/fonts/TTF/IBMPlexMono-Medium.ttf \
    -background black \
    -fill white \
    -size 1366x768 \
    -gravity center \
    -pointsize 24 \
    caption:"$(sholat) \n $(hijri)" ~/.local/share/wallpaper/wallpaper.png

display -window root $WALLPAPER 
